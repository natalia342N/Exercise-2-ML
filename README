# Exercise-2-ML

This project contains solutions and code for Exercise 2 of a Machine Learning course.

## Project Structure

- `src/` - Source code for the machine learning exercises
- `data/` - Datasets used in the exercises
- `notebooks/` - Jupyter notebooks for exploration and analysis
- `README` - Project overview and instructions

## Getting Started

1. Clone the repository:
    ```
    git clone https://github.com/natalia342N/Exercise-2-ML.git
    ```
2. Install dependencies:
    ```
    pip install -r requirements.txt
    ```

    Alternative use conda:
    ```
    conda env create -f environment.yml
    ```
3. Explore the notebooks or run `main.py`.

## Requirements

- Python 3.8+
- See `requirements.txt` for additional dependencies
